// import { fromJS } from 'immutable';
// import { makeSelectPageADomain } from '../selectors';

// const selector = makeSelectPageADomain();

describe('makeSelectPageADomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
