// import { fromJS } from 'immutable';
// import { makeSelectPageBDomain } from '../selectors';

// const selector = makeSelectPageBDomain();

describe('makeSelectPageBDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
